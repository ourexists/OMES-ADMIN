<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ourexists.omes.device.mapper.EquipRealtimeMapper">

    <insert id="upsert">
        INSERT INTO t_equip_realtime (
            id, tenant_id, self_code, gw_id,
            online_state, run_state, alarm_state,
            online_change_time, run_change_time, alarm_change_time,
            workshop_code, name, equip_time, alarm_level,
            payload
        ) VALUES (
            #{row.id}, #{row.tenantId}, #{row.selfCode}, #{row.gwId},
            #{row.onlineState}, #{row.runState}, #{row.alarmState},
            #{row.onlineChangeTime}, #{row.runChangeTime}, #{row.alarmChangeTime},
            #{row.workshopCode}, #{row.name}, #{row.equipTime}, #{row.alarmLevel},
            CAST(#{row.payload, typeHandler=com.baomidou.mybatisplus.extension.handlers.JacksonTypeHandler, jdbcType=OTHER} AS jsonb)
        )
        ON CONFLICT (id) DO UPDATE SET
            tenant_id = EXCLUDED.tenant_id,
            self_code = EXCLUDED.self_code,
            gw_id = EXCLUDED.gw_id,
            online_state = EXCLUDED.online_state,
            run_state = EXCLUDED.run_state,
            alarm_state = EXCLUDED.alarm_state,
            online_change_time = EXCLUDED.online_change_time,
            run_change_time = EXCLUDED.run_change_time,
            alarm_change_time = EXCLUDED.alarm_change_time,
            workshop_code = EXCLUDED.workshop_code,
            name = EXCLUDED.name,
            equip_time = EXCLUDED.equip_time,
            alarm_level = EXCLUDED.alarm_level,
            payload = EXCLUDED.payload
    </insert>
</mapper>
