# 第三方组件与许可证

根目录的 Apache-2.0 许可证适用于本项目自有内容，不替代第三方组件的许可证，也不更改其版权归属。

## 组件来源

- Java 依赖：根目录及各模块的 `pom.xml`，包括 Era 框架和传递依赖。具体许可证以对应版本制品中的 POM、LICENSE 和 NOTICE 为准。
- 前端依赖：`omes-iomp-web-admin/package.json`、`omes-iomp-app/package.json` 及各自锁文件。依赖包的许可证字段和许可证文件保持其原有声明。
- 移动端内置组件：`omes-iomp-app/uni_modules/` 中的组件，包括 `cool-unix`、`lime-echart`，以及其他带有独立版权或授权声明的资源，按原作者提供的条款使用。

## 分发时的处理

发布源码、安装包、前端静态资源或容器镜像时，应根据实际包含的组件及版本整理第三方许可证和必要的归属声明，并随分发物提供。不要用本项目的 Apache-2.0 声明覆盖依赖包或内置组件的声明。

本文是组件来源和维护说明，不是全部依赖的许可证清单，也不表示第三方组件已完成授权兼容性审查。新增或升级依赖时，应同步检查对应版本的授权条款并保留必要声明。
